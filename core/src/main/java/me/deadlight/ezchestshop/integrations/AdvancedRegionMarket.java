package me.deadlight.ezchestshop.integrations;

import org.bukkit.event.Listener;

/**
 * Legacy AdvancedRegionMarket integration stub.
 *
 * PebbleShop is built for Pebble Quest/PebbleCore and no longer depends on
 * AdvancedRegionMarket's external API at compile time. Keeping this class as a
 * no-op preserves internal references without forcing CI to resolve the old ARM
 * Maven artifact.
 */
public class AdvancedRegionMarket implements Listener {
}
